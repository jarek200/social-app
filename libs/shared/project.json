{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "shared",
  "root": "libs/shared",
  "sourceRoot": "libs/shared/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx biome lint libs/shared"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest --config libs/shared/vitest.config.ts"
      }
    }
  },
  "tags": [
    "scope:shared",
    "type:library"
  ]
}
